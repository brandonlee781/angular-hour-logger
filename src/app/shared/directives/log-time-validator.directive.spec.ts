import { LogTimeValidatorDirective } from './log-time-validator.directive';

describe('LogTimeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LogTimeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
